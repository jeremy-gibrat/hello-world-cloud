# Fichier .env - Configuration de l'application

Ce fichier contient les variables d'environnement sensibles et la configuration de l'application.

## 📝 Configuration

1. **Copiez le fichier d'exemple**:
   ```bash
   cp .env.example .env
   ```

2. **Éditez `.env` avec vos valeurs**:
   - `GHCR_USERNAME`: Votre nom d'utilisateur GitHub
   - `GHCR_TOKEN`: Votre Personal Access Token GitHub avec les scopes `read:packages` et `write:packages`
   - Variables Azure selon votre configuration

## 🔒 Sécurité

- ✅ `.env` est dans `.gitignore` et ne sera **jamais** commité
- ✅ `terraform/terraform.tfvars` est généré automatiquement depuis `.env`
- ✅ Utilisez `.env.example` comme template (sans secrets)

## 🚀 Utilisation

Les scripts suivants utilisent automatiquement `.env`:
- `./build-and-push-azure.sh` - Build et push des images Docker
- `./azure-deploy.sh` - Déploiement sur Azure AKS
- `./tunnel.sh` - Création de tunnels kubectl

## 📦 Génération de terraform.tfvars

Le fichier `terraform/terraform.tfvars` est généré automatiquement depuis `.env`:
```bash
cd terraform
./generate-tfvars.sh
```

Ou automatiquement lors de `./azure-deploy.sh`.
